#ifdef _LIST_HPP_
#define _LIST_HPP_

#include "Item.hpp"
#include <list>

class List{
private:
    // List which stores Item objects (Grocery items)
    //std::list<Item> _list;
public:
    // Default constructor
    
    
    // Accessors
    // Modifiers
};
#endif // DEBUG
